#!/usr/bin/env bash

set -e

cd "$(dirname "$0")"

if [ ! -t 0 ] || [ ! -t 1 ] || [ ! -t 2 ]; then
    DISABLE_TTY="-T"
fi

./docker-compose exec -e XDEBUG_SESSION_START=1 $DISABLE_TTY --user www-data wordpress ./vendor/bin/wp "$@"
