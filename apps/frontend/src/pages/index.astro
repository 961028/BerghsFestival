---
import { getEntry } from "astro:content";
import Layout from "../layouts/Layout.astro";

const settings = await getEntry("settings", "0");
---

<Layout title="Welcome">
	<h1>{settings.data.title}</h1>
</Layout>
