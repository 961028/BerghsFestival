---
import Layout from '../layouts/Layout.astro';
import { getSettings } from '../lib/wp-api';

const settings = await getSettings();
---

<Layout title="Welcome">
	<h1>{settings.title}</h1>
</Layout>
